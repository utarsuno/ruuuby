

### Math Notation

```for anything not yet formally added into `Ruuuby` but useful to document ahead of time```

 - `⌈x⌉ = min{n ∈ ℤ | n >= x}`
   - ceiling: `⌈x⌉ = n`
 - `⌊x⌋ = max{m ∈ ℤ | m <= x}`
   - floor: `⌊x⌋ = n`
