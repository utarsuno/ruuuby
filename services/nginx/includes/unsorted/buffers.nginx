# max buffer size for body getting sent to NGINX
client_body_buffer_size     10K;

# max buffer size for header getting sent to NGINX
client_header_buffer_size   1k;

# Client request maximum size
client_max_body_size        8m;

# maximum number + size for large header buffers
large_client_header_buffers 2 2k;