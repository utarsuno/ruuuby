#!/usr/bin/env bash
# encoding: UTF-8

export RUUUBY_FULL_DEBUG="t"

set -euo pipefail
IFS=$'\n\t'
set -vx

bundle exec rake compile:ruby_class_mods
bundle exec rake rdoc
bundle exec rake rspec_all
bundle exec rake build
