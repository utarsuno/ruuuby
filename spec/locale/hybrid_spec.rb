# coding: UTF-8

RSpec.describe 'ruby' do
  context 'locale', :locale do
    context 'ruby-engine-cache' do
      # TODO: need to update so this check doesn't get run during a full QA run
      #it 'lazy-loaded DB correctly' do
      #  expect(💎.engine.orm.state_flag).to eq(1)
      #end
    end
  end
end
